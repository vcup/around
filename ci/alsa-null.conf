# ALSA virtual null device — discards audio, always available
# Used for CI/testing environments without real audio hardware.
# Usage: ALSA_CONFIG_PATH=ci/alsa-null.conf cargo test
pcm.!default {
    type null
}
